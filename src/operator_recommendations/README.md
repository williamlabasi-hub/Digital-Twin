
# Operator recommendations

The implemented prototype course-of-action advisory engine lives in `coa`.
It combines validated health, object-identification, and collision-risk
evidence into review-only operator advisories.

The engine has no command authority and cannot authorize maneuvers. See
`docs/requirements/coa/README.md` for its contracts and decision rules.
